import { BifrostTypo } from "@/components/steins-gate/ui/bifrost-typo";

export function NavbarBrand() {
  return (
    <a href="#" aria-label="Operation Bifrost — back to top" className="flex items-center">
      <BifrostTypo className="h-12" />
    </a>
  );
}
